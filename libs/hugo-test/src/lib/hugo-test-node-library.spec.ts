import { hugoTestNodeLibrary } from './hugo-test-node-library';

describe('hugoTestNodeLibrary', () => {
  it('should work', () => {
    expect(hugoTestNodeLibrary()).toEqual('hugo-test-node-library');
  });
});
