import { sharedInterfacesEdcApiToDb } from './shared-interfaces-edc-api-to-db';

describe('sharedInterfacesEdcApiToDb', () => {
  it('should work', () => {
    expect(sharedInterfacesEdcApiToDb()).toEqual('shared-interfaces-edc-api-to-db');
  });
});
