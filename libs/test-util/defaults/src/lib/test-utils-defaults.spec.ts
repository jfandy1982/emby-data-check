import { testUtilsDefaults } from './test-utils-defaults';

describe('testUtilsDefaults', () => {
  it('should work', () => {
    expect(testUtilsDefaults()).toEqual('test-utils-defaults');
  });
});
