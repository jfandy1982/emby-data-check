name: Continuous Integration Pipeline

on:
  push:

  pull_request:
    types:
      - opened
      - reopened
      - synchronize

  workflow_dispatch:
    inputs:
      edc-api-manual:
        description: 'Is app [edc-api] affected?'
        required: false
        type: boolean
        default: false

      edc-ui-ng-manual:
        description: 'Is app [edc-ui-ng] affected?'
        required: false
        type: boolean
        default: false

permissions:
  actions: read
  attestations: read
  checks: read
  contents: read
  deployments: read
  id-token: none
  issues: write
  discussions: read
  packages: read
  pages: read
  pull-requests: write
  repository-projects: read
  security-events: read
  statuses: read

jobs:
  static-checks:
    name: 'Static Code Checks'

    runs-on: ubuntu-latest

    steps:
      - name: 'Prepare repository and initialize Node.js environment'
        uses: ./.github/actions/setup-repository-workspace
        with:
          need-cache-npm: true
          need-cache-cypress: false

      - name: 'Lint all projects'
        run: npm run lint:all:check
