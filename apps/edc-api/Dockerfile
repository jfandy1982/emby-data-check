# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build edc-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t edc-api`.
#FROM docker.io/node:20.10.0-alpine AS builder
FROM docker.io/node:20.10.0-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system edc-api && \
  adduser --system -G edc-api edc-api

COPY dist/apps/edc-api edc-api
# RUN chown -R edc-api:edc-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
# RUN npm --prefix edc-api --omit=dev -f install

# CMD [ "node", "edc-api" ]
